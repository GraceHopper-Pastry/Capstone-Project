"use strict";

const {
    db,
    models: { User, Offerings, Shop, Review, Booking }
} = require("../server/db");

/**
 *  This file as been created as a ref resource displaying
 * each db model and its associated magic methods based on
 * our provided associations in ./index
 *
 *
 * To console log the model's magic methods, we can use:
 *
 * console.log(Object.keys(ModelName.prototype))
 */

/*
 * MODEL: { USER }
*/

user.keys(User.prototype);

/*
 * MODEL: { OFFERINGS }
*/

/*
 * MODEL: { SHOP }
*/

/*
 * MODEL: { REVIEW }
*/

/*
 * MODEL: { BOOKING }
*/
